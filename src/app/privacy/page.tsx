import type { Metadata } from "next";
import { ComingSoon } from "@/components/ComingSoon";

export const metadata: Metadata = { title: "プライバシーポリシー | XTRUST" };

export default function Page() {
  return <ComingSoon title="プライバシーポリシー" />;
}
