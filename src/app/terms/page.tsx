import type { Metadata } from "next";
import { ComingSoon } from "@/components/ComingSoon";

export const metadata: Metadata = { title: "利用規約 | XTRUST" };

export default function Page() {
  return <ComingSoon title="利用規約" />;
}
